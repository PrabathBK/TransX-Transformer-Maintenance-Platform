# --- MongoDB (development) ---
# Local instance (no login)
spring.data.mongodb.uri=mongodb://localhost:27017/transx

# If using Atlas instead, comment out the above and use:
# spring.data.mongodb.uri=mongodb+srv://<user>:<password>@cluster0.abcde.mongodb.net/transx

# --- File storage & public URL ---
app.storage.root=uploads
app.server.public-base-url=http://localhost:8080

# --- CORS: allow your Vite dev server ---
app.cors.allowed-origins=http://localhost:5173